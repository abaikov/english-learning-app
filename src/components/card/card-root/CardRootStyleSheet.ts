import { StyleSheet } from 'react-native';

export const CardRootStyleSheet = StyleSheet.create({
    cardRoot: {
        position: 'absolute',
        top: 0,
        right: 0,
        bottom: 0,
        left: 0,

        display: 'flex',
        flex: 1,

        padding: 16,
    }
})

import { StyleSheet } from 'react-native';
import { EProjectColor } from '../../palette/EProjectColor';

export const CardTranslationStyleSheet = StyleSheet.create({
    root: {},
    text: {
        color: EProjectColor.WHITE,
        fontSize: 20,
    },
})

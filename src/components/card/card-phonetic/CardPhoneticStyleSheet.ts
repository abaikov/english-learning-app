import { StyleSheet } from 'react-native';

export const CardPhoneticStyleSheet = StyleSheet.create({
    root: {
        marginTop: 2,
        color: '#58B',
        textAlign: 'center',
        fontSize: 14,
    }
})

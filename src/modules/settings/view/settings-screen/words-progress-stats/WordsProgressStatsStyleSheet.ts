import { StyleSheet } from 'react-native';
import { EProjectColor } from '../../../../../components/palette/EProjectColor';

export const WordProgressStatsStyleSheet = StyleSheet.create({
    textBlock: {
        paddingBottom: 16,
    },
    text: {
        color: EProjectColor.WHITE,
    },
    titleText: {
        color: EProjectColor.WHITE,
        paddingBottom: 16,
        // paddingLeft: 16,
    }
});
